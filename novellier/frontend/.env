# Frontend environment variables
# Example: REACT_APP_API_URL=http://localhost:5000/api
# These are typically not committed to the repository, especially if they contain sensitive information.
# For create-react-app, variables must start with REACT_APP_ to be embedded.
